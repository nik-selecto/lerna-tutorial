{
  "name": "im-learning-lerna",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "publish": "npx lerna run build && npx lerna run publish"
  },
  "private": false,
  "devDependencies": {
    "@types/node": "^17.0.32",
    "lerna": "^4.0.0",
    "typescript": "^4.6.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nik-selecto/lerna-tutorial.git"
  }
}
